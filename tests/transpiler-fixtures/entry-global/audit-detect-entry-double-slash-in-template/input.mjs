// `//` inside a template literal (backtick-quoted). same protection as single/double
// quotes - the line-comment detector skips through template-literal content tracking
// the opening backtick to its closing one (may span multiple lines for templates).
var x = `https://example.com`;
import 'core-js/actual/array/from';
[arr]()
