// bundler-emitted entry id with a doubled slash (Farm path-join artifact) -
// must canonicalise to the single-slash form so entry expansion + dedup land
// on the same logical core-js path
import "core-js/modules/es.array.at";
import "core-js/modules/es.array.of";