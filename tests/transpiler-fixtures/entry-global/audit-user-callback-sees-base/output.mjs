// the user-supplied `shouldInjectPolyfill` callback receives the base polyfill name,
// not a transformed alias - parity with how compat data exposes module identifiers.
import "core-js/modules/es.array.at";