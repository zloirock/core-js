// A scheduled computed slot and a consumed static share one exported declaration.
// Initializers and retained properties stay in source order.
// Only the source bindings are exported, including every extracted binding.
export const lead = before(), { [(key(), 'at')]: at, custom } = make(), mid = after(), { from } = Array, tail = last();
