// A JSX prop expression hands its runtime value to the renderer. Both flavors
// provide the constructor family, while the tag and attribute names name no values.
const element = <host value={Map} />;
const instance = new Map();
