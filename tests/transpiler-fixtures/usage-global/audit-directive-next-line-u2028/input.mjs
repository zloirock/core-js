// `buildOffsetToLine` recognises U+2028/U+2029 as line terminators per ES spec;
// a `core-js-disable-next-line` comment before a U+2028-separated expression should
// skip the following line only
// core-js-disable-next-line
[].at(-1);
[].at(-1);
