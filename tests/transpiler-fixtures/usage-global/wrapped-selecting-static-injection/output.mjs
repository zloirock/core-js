import "core-js/modules/es.symbol.iterator";
import "core-js/modules/es.object.from-entries";
import "core-js/modules/es.object.group-by";
import "core-js/modules/es.object.has-own";
import "core-js/modules/es.object.to-string";
import "core-js/modules/es.array.iterator";
import "core-js/modules/es.array.from";
import "core-js/modules/es.math.trunc";
import "core-js/modules/es.string.iterator";
import "core-js/modules/web.dom-collections.iterator";
// a SELECTING receiver under a WRAPPER injects the static the constructor arm may hand over, the
// way the bare init does - the level's slot pairs with the host's literal by position or by plain
// key, and the arm's static is a claim. one module per row, attributable to its own host shape
const [{
  from
}] = [c ? Array : userObj];
const [{
  fromEntries
} = {}] = [c && Object];
const {
  w: {
    groupBy
  }
} = {
  w: c ? Object : userObj
};
function viaParamDefault([{
  hasOwn
}] = [c ? Object : userObj]) {
  return hasOwn;
}
const [[{
  trunc
}]] = [[c ? Math : userObj]];
export { from, fromEntries, groupBy, viaParamDefault, trunc };