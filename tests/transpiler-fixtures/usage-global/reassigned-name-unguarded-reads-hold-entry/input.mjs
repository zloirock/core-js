// a reassigned name is served through the identity guard its host renders, over values that guard
// can name. an `in` test, a slot default, a for-of head and a parameter's `in` render none, and a
// getter with an effect is no value the guard names: each keeps the whole pure entry of what it may
// hold, and usage-global injects the static it reads off each
let probed = Set;
if (flag) probed = Map;
use('groupBy' in probed);
let filled = null;
filled ||= Promise;
use('try' in filled);
let defaulted = Set;
if (flag) defaulted = Iterator;
const { from = null } = defaulted;
let iterated = Set;
if (flag) iterated = URL;
for (const { canParse } of [iterated]) use(canParse);
class Getters { static get made() { log(); return Symbol; } }
let got;
got = Getters.made;
use(got.for);
function param(value) { if (flag) value = AggregateError; return 'isError' in value; }
param(Set);
