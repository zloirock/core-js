import _Array$from from "core-js-pure/actual/array/from";
// `package: 'core-js-pure'` (legacy npm name) — resolver routes imports through the
// lowercased alias; `@core-js/pure` default is overridden. user sources this alias directly
_Array$from(x);