import _at from "@core-js/pure/actual/instance/at";
var _ref3;
// Captures stay local between the original exported sibling initializers.
// The export set contains only the source bindings, with computed key reads in order.
export const lead = before();
const _ref2 = make();
const {
  [(outer(), 'w')]: _ref
} = null == _ref2 ? _ref2[""] : _ref2;
export const method = (_ref3 = _ref, null == _ref3 ? _ref3[""] : (leaf(), _at(_ref3)));
export const tail = after();