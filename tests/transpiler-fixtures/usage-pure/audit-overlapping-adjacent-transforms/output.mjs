import _Promise$resolve from "@core-js/pure/actual/promise/resolve";
import _Promise$all from "@core-js/pure/actual/promise/all";
// two adjacent rewrite sites whose source spans touch: the rewrite must apply both
// without overlapping or losing characters between them.
_Promise$resolve(1);
_Promise$all([]);