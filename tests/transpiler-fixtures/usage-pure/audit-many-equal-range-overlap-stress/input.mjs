// 5 chained instance polyfills in a single arrow body - a nested-rewrite stress shape:
// every claim shares the one receiver chain and emission must stay stable
const f = x => arr.flat().at(0).findLast(p).findLastIndex(p2).includes(z);
