#!/usr/bin/env node
// a directive on the line under a hashbang is shared between the hashbang and the first statement,
// and the injected import block lands between them: the directive has to stay with the statement it
// marks, or it guards the import instead and the opted-out read is claimed by the next pass
// core-js-disable-next-line
use(a.at(0));
export const after = b.flat();
