import _atMaybeArray from "@core-js/pure/actual/array/instance/at";
import _globalThis from "@core-js/pure/actual/global-this";
import _toFixedMaybeNumber from "@core-js/pure/actual/number/instance/to-fixed";
import _Number$MAX_SAFE_INTEGER from "@core-js/pure/actual/number/max-safe-integer";
import _self from "@core-js/pure/actual/self";
var _ref, _ref2, _ref3, _ref4, _ref5;
// an erase claim widens its span past the node to swallow the optional token of the hop above it,
// so the survivor reads plainly. that leaves a needle ending in a lone `?`, which an enclosing
// render that deoptionalized the same hop no longer carries - it has to be located without the
// token. dotted and computed survivors take a different number of characters, and the sequence /
// chain-assign roots reach the same claim through their own render
function g() { return _globalThis; }
function eff() {}
const dh = () => _globalThis;
let w;
let x = [];
export const dottedTail = null == (_ref = _Number$MAX_SAFE_INTEGER) ? void 0 : _toFixedMaybeNumber(_ref).call(_ref, 2);
export const computedTail = null == (_ref2 = _Number$MAX_SAFE_INTEGER) ? void 0 : _toFixedMaybeNumber(_ref2).call(_ref2, 2);
export const seqRoot = null == (_ref3 = (eff(), _Number$MAX_SAFE_INTEGER)) ? void 0 : _toFixedMaybeNumber(_ref3).call(_ref3, 2);
export const plainSeqRoot = null == (_ref4 = (eff(), _Number$MAX_SAFE_INTEGER)) ? void 0 : _toFixedMaybeNumber(_ref4).call(_ref4, 2);
export const assignRoot = null == (_ref5 = (w = _globalThis, _Number$MAX_SAFE_INTEGER)) ? void 0 : _toFixedMaybeNumber(_ref5).call(_ref5, 2);
export const collapsedHopClaim = _self == null ? void 0 : _atMaybeArray(_self.Array.prototype).call(x, 0);