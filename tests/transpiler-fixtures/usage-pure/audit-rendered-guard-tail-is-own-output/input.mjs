// the alternate a nav-collapse render emits is OWN OUTPUT: the tail hop the plan deliberately kept
// is re-entered ON that hop, and collapsing it there folds a step further than any spelling of this
// source asks for. the single-member twin beside it is the shape both emitters keep
export const twoMembersBelow = globalThis.window?.self.window.userSlot.deeper;
export const oneMemberBelow = globalThis.window?.self.window.userSlot;
