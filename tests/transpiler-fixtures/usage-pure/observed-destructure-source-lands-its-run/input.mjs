// a destructure source whose pattern DOES claim, under a carrier that observes the value: the
// mirror declines (the other arm carries no such key), so the receiver stays spelled - and a run
// kept spelled lands on the deepest span pure can back, the hops a member reads through folding
// onto it. the last row is the negative: a probe TERMINAL in the observed value keeps its slot.
const { from } = globalThis.self.Array && {};
const { isInteger } = globalThis.self.window.Number && {};
const { getOwnPropertyNames } = globalThis.window.self.Object && {};
const { fromEntries } = globalThis.window.Object && {};
const { getPrototypeOf } = globalThis.self.window && {};
export default [from, isInteger, getOwnPropertyNames, fromEntries, getPrototypeOf];
