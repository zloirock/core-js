// a memo-bearing sibling claims the whole-declaration render; the SE-key trailing polyfill
// declarator anchored at its host declarator's end must bake into that render, not crash it
const { at, other } = getArr(), { [(e1(), 'flat')]: f } = arr;
export const { findLast } = getList(), { [(e2(), 'flatMap')]: fm } = arr2;
const { includes } = getSet(), { [(e3(), 'toReversed')]: tr } = arr3, { tail } = obj;
// SE key FIRST: the pair appends after its residual declarator, never inside a wrapped init
const { [(e4(), 'toSpliced')]: ts } = arr4, { findLastIndex } = getColl();
console.log(at, other, f, fm, findLast, includes, tr, tail, ts, findLastIndex);
