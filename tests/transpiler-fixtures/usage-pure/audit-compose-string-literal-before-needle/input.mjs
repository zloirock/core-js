// string literal containing text that looks like a rewrite needle, just before the
// real rewrite site: must not trigger a spurious replacement inside the string.
arr.includes("arr.at(0)", arr.at(0));
