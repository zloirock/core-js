import _globalThis from "@core-js/pure/actual/global-this";
import _Number$MAX_SAFE_INTEGER from "@core-js/pure/actual/number/max-safe-integer";
// a SEAL ends nothing for the hop run reading THROUGH it: the paren the source wrote around a
// navigation is printer trivia, and stopping the collapse's upward climb on it left the run
// half-folded (`_globalThis.window.X`) where the unsealed twin folds whole. the climb reads the
// transparent-wrapper canon, so the cast that spells the same seal steps aside with the paren
let out, sealed, plain, backed;
plain = _globalThis.noSuchStatic;
sealed = _globalThis.noSuchStatic;
backed = _Number$MAX_SAFE_INTEGER;
out = [plain, sealed, backed];
export const read = out;