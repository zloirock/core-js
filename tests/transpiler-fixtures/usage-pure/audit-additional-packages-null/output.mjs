import _Array$from from "@core-js/pure/actual/array/from";
// `additionalPackages: null` passes validation (explicit null bypass for conditional configs)
// and the resolver treats it the same as undefined - only the main `package` is in play
_Array$from(x);