// trailing comma after last property (before rest if any) - parser accepts it as
// non-rest. tests `getPropRemovalRange` slicing handles trailing comma without leaving
// double commas in the rebuilt pattern
const { from, of, } = Array;
export { from, of };
