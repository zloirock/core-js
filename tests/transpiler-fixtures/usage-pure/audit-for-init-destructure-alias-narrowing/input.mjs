// a for-init destructure extraction registers the body-extract alias like the
// statement-level channel, so the local narrows return types to the receiver-specific
// helper instead of the generic instance dispatcher
for (const { from } = globalThis.Array; cond(); ) use(from([6]).at(0));
