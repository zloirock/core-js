// The census closes and the receiver still must be NAMED: an argument naming no static, and a
// host the file cannot see every caller of, both keep the parameter raw - a leaf default there
// would answer where the source hands back `undefined`.
function unnamed({
  groupBy: a
}) {
  return a;
}
export function escaped({
  of: b
}) {
  return b;
}
export const x = unnamed({});
export const y = escaped(Array);