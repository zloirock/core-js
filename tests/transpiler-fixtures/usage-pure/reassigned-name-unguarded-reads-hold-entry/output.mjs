import _AggregateError from "@core-js/pure/actual/aggregate-error";
import _Iterator from "@core-js/pure/actual/iterator";
import _Map from "@core-js/pure/actual/map";
import _Promise from "@core-js/pure/actual/promise";
import _Set from "@core-js/pure/actual/set";
import _Symbol from "@core-js/pure/actual/symbol";
import _URL from "@core-js/pure/actual/url";
// a reassigned name is served through the identity guard its host renders, over values that guard
// can name. an `in` test, a slot default, a for-of head and a parameter's `in` render none, and a
// getter with an effect is no value the guard names: each keeps the whole pure entry of what it may
// hold, and usage-global injects the static it reads off each
let probed = _Set;
if (flag) probed = _Map;
use('groupBy' in probed);
let filled = null;
filled ||= _Promise;
use('try' in filled);
let defaulted = _Set;
if (flag) defaulted = _Iterator;
const {
  from = null
} = defaulted;
let iterated = _Set;
if (flag) iterated = _URL;
for (const {
  canParse
} of [iterated]) use(canParse);
class Getters {
  static get made() {
    log();
    return _Symbol;
  }
}
let got;
got = Getters.made;
use(got.for);
function param(value) {
  if (flag) value = _AggregateError;
  return 'isError' in value;
}
param(_Set);