import _Array$from from "@core-js/pure/actual/array/from";
import _globalThis from "@core-js/pure/actual/global-this";
import _self from "@core-js/pure/actual/self";
// how far the claimless call-rooted plan climbs is asked by the canon the FOLD obeys, not by the
// hop's bare NAME: a MUTATED realm slot holds the user's own object, so the run ENDS below it and
// its read stays spelled. climbed by the name alone the plan was handed a run one hop too long and
// the read folded away with it
_globalThis.window = {};
const dh = () => _globalThis;
export const mutatedHopEndsTheRun = _self.window.Array.from([1]);
export const mutatedHopUnderATail = _self.window.customQ;

// NEGATIVE: a run of PRISTINE hops in the same file - the slot write deoptimizes the name it
// writes and no other, so the climb runs through these and the whole navigation folds
export const pristineRun = _Array$from([2]);