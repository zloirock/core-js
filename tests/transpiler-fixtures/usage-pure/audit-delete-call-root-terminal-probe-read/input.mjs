// the READ twin of the delete rows beside it: the same run in value position IS the environment
// probe the source asked for, so every hop keeps its slot over the deepest span pure can back
const dhRoot = () => globalThis;
export const readTerminalProbe = dhRoot().self.window;
export const readTerminalProbeNav = dhRoot().self.window.customQ;
