import _getIteratorMethod from "@core-js/pure/actual/get-iterator-method";
import _Math$sumPrecise from "@core-js/pure/actual/math/sum-precise";
import _Object$groupBy from "@core-js/pure/actual/object/group-by";
import _Symbol$asyncIterator from "@core-js/pure/actual/symbol/async-iterator";
// an ASSIGNMENT host and the computed-key reads keep a getter-read prefix element the way the
// declaration hosts do: in the rescue the discard owes, and in a key or a receiver whose sequence the
// fold collapses - in the order the source ran each
class K {
  static get g() {
    log();
    return 0;
  }
}
function mkObject() {
  log();
  return Object;
}
let a2;
K.g, mkObject();
a2 = _Object$groupBy;
const iter = (K.g, _getIteratorMethod([1, 2]));
const folded = (K.g, _Math$sumPrecise);
const receiverIter = [1, 2][K.g, _Symbol$asyncIterator];
use(a2, iter, folded, receiverIter);