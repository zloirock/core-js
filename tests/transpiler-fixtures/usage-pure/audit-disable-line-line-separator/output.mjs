import _at from "@core-js/pure/actual/instance/at";
// line-level disable directive followed by a Unicode LINE SEPARATOR: the directive's
// scope ends at the next semantic line.
const a = _at(arr).call(arr, 0);
// core-js-disable-next-line
const b = arr.at(0);