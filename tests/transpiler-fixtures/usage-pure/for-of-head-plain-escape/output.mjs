import _Map from "@core-js/pure/actual/map";
// A plain loop binding holds the literal element just as a destructured one does.
// An unknown consumer can read any static on the substituted constructor.
for (const value of [_Map]) hand(value);