// an assignment DISCARDED as a non-tail sequence element: nobody reads the value it yields, so the
// position is as free as a statement - and the legs do NOT agree on what may be claimed there. this
// leg consumes an instance claim off a proxy surface; the other keeps the destructure whole, because
// a consume in this position owes a proof the statement routes encode and this one does not have:
// which READS the raw slot performs, and whether the dispatch re-performs every one of them (a
// receiver's getter, a name match, a key effect). the sidecar records the divergence until it does
import _flatMaybeArray from "@core-js/pure/actual/array/instance/flat";
import _globalThis from "@core-js/pure/actual/global-this";

let m, taken;
const zd = (m = _flatMaybeArray(_globalThis.Array.prototype), 7);

taken = zd;

export { m, taken };