import _self from "@core-js/pure/actual/self";
import _Set from "@core-js/pure/actual/set/constructor";
// a destructure source reached through a value-OBSERVING carrier lands its run like every other
// receiver: a hop pure can back is erasable anywhere, and the `?.` over it dies with the landing.
// the chain MARKER stands between the nav and the carrier and is stepped through like any other
// wrapper, on either side of it. the rows differ only in the carrier, so one raw hop among them
// would show up as its own import
let flag;
export const {
  zzz: viaLogicalAnd
} = flag && _self.Array;
export const {
  zzz: viaTernaryArm
} = flag ? _self.Array : _Set;
export const {
  zzz: viaLogicalOr
} = _self.Array || _Set;
export const {
  zzz: viaPlainNav
} = flag && _self.Array;
export const {
  zzz: viaSequenceTail
} = (0, _self.Array);