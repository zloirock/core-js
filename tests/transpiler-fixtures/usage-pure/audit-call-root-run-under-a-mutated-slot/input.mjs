// a claimless CALL-rooted probe run whose slot the file itself WROTE: the write deopts the claim,
// and what stands above the run then reads THROUGH it - so the whole navigation folds onto the
// root ponyfill, the identifier twin's bytes, in every consumer position. the run's own terminal
// spelling never enters it: the fold is decided by what reads the run, not by the last hop
globalThis.customCallSlot = 1;
const dh = () => globalThis;
export const identTwin = typeof (globalThis.self.window).customCallSlot;
export const viaCallTypeof = typeof (dh().self.window).customCallSlot;
export const viaCallRead = (dh().self.window).customCallSlot;
export const viaCallDelete = delete (dh().self.window).customCallSlot;
