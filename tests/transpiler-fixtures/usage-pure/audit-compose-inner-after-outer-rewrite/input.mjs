// nested rewrite with an inner site whose text appears after an outer rewrite
// boundary: ordering between outer and inner replacements must produce stable output.
Promise.resolve(obj.at(0));
Promise.resolve(obj.at(0));
