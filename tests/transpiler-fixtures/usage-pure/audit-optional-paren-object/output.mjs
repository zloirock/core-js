import _Promise from "@core-js/pure/actual/promise/constructor";
// optional access on a parenthesised object expression `(obj)?.method`: the parens
// must be peeled for the rewrite to recognise the receiver.
_Promise;