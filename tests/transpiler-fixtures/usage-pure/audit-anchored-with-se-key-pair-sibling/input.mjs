// an anchored hop slot splits statement-per-declarator while the SE-key TDZ pair (the
// receiver-bound host and its trailing polyfill declarator) stays in ONE statement
const { Map: { custom } } = globalThis, { [(eff(), 'at')]: a } = arr;
console.log(custom, a);
