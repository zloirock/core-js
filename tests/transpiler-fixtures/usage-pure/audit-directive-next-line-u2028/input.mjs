// disable-next-line directive followed by U+2028 line terminator (ES spec line
// terminator distinct from \n). plugin must recognise the directive applies to the
// next statement, not the same one
// core-js-disable-next-line const flat = [1, [2]].flat();
console.log(flat);
