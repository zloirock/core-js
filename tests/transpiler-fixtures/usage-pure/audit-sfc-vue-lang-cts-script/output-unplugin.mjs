var _Array$from = require("@core-js/pure/actual/array/from");
var _at = require("@core-js/pure/actual/instance/at");
var _replaceAllMaybeString = require("@core-js/pure/actual/string/instance/replace-all");
// Vue SFC virtual id with `lang=cts` selects the CommonJS+TS dialect in unplugin emit (`require`).
// babel-plugin sees no virtual id and falls back to ESM (`import`); both must accept the `as` cast and emit polyfills.
const x = 1 as number;
_at(arr).call(arr, 0);
_replaceAllMaybeString(str).call(str, 'a', 'b');
_Array$from(it);