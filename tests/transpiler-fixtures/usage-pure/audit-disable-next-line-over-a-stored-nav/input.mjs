// the opt-out over a nav the source STORES: the stored value renders from the detector's own
// suppression points, ahead of the claim channel, so it has to ask the directive itself. the
// second read is the positive control - the same shape one line down still polyfills
let c = 0;
let kept;
let live;
// core-js-disable-next-line -- the caller ships its own realm handling here
export const a = (kept = (c++, globalThis.window?.self))?.Promise.race;
export const b = (live = (c++, globalThis.window?.self))?.Promise.race;
