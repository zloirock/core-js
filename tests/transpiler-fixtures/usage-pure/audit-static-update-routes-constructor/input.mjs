// an update mutation routes its receiver through the injected constructor (read and write
// halves share the object); an unrelated clean static in the same file keeps its ordinary
// receiver-less import
Promise.allSettled++;
Promise.allSettled([p]);
Map.groupBy([], x => x);
