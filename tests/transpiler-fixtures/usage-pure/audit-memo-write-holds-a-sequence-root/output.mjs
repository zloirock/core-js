import _globalThis from "@core-js/pure/actual/global-this";
import _at from "@core-js/pure/actual/instance/at";
import _Promise from "@core-js/pure/actual/promise/constructor";
import _self from "@core-js/pure/actual/self";
// the memo a dispatch mints over a SEQUENCE root stores that sequence: read through the assign peel
// alone the binding resolved to nothing, and every claim spelled off it stayed raw off the ponyfill.
// the chain-ROOT peel answers what the memo holds, so the constructor below the dispatch resolves.
// the last row roots the navigation in an alias the SAME sequence writes: the write dominates the
// read beside it, which is the one flow fact the surface question needs - without it the memo held
// an unprovable surface and the constructor read raw off it
let g, v, key;
export const seqRootMemo = null == (g = _globalThis, v = null == _globalThis.window ? void 0 : _self) ? void 0 : _at(_Promise[key]);
export const seqPrefixMemo = null == (log(), null == _globalThis.window ? void 0 : _self) ? void 0 : _at(_Promise[key]);
export const aliasWrittenBeside = null == (g = _globalThis, v = null == g.window ? void 0 : _self) ? void 0 : _at(_Promise[key]);
function log() {}
export { g, v };