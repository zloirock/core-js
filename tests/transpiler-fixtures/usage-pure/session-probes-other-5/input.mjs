// probe corpus of the defense cycles over the destructure wrappers, family "other", part 5:
// every block is one probed form, self-contained over the header bindings, locked on both legs
let pick = 1;
const c = 1;
const userObj = {};
const arr = [1, 2];
const nb = { y: arr };
const rest = [];
const more = {};
const wrapped = [Object];
const log = [];
const obj = {};
const rows = [];
const k = 'k';
let kw;
const nul = null;
function eff() { return Object; }
function eff2() {}
function mark(t, v) { log.push(t); return v; }

{
const { Array: { from: { length: L } = fb } } = globalThis; L();
}
{
const { Array: { prototype: { values: v } }, ...r } = globalThis;
}
{
const { Array: { prototype: { values: v1 } } } = globalThis;
}
{
const { KEY: { at: m }, ...rest } = { w: [1, 2], z: 1 }; use(m, rest);
}
{
const { Map: M = fb } = globalThis; M();
}
{
const { ['w']: { Map: m } } = { w: globalThis }; use(m);
}
{
const { ['w']: { from: f } } = { w: pick ? Array : userObj };
}
{
const { [('w')]: { at: a } } = { w: src };
}
{
const { [(e7(), 'with')]: w7 = dfltF() } = [9]; use(w7);
}
{
const { [(e7(), 'with')]: w7 = dfltG() } = eff(); w7();
}
{
const { [(e7(), 'with')]: w7 = dfltG(), [(e8(), 'toSpliced')]: t8 } = [9]; w7(t8);
}
{
const { [(e7(), 'with')]: w7 = dfltG(), [(e8(), 'toSpliced')]: t8 } = eff(); w7(t8);
}
{
const { [(eff('k'), 'Array')]: { [(eff('k2'), 'from')]: f } } = globalThis;
}
{
const { [(eff('k'), 'Array')]: { [Symbol.iterator]: it16 } } = globalThis;
}
{
const { [(eff('k'), 'Array')]: { from: f } } = globalThis;
}
{
const { [(eff('k'), 'Array')]: { from: f1 } } = globalThis;
}
{
const { [(eff('k'), 'Array')]: { from: f11 } = {} } = globalThis;
}
{
const { [(eff('k'), 'Array')]: { from: f15 = 1 } } = globalThis;
}
{
const { [(eff('k'), 'Array')]: { from: f5 }, z } = globalThis;
}
{
const { [(eff('k'), 'Array')]: { from: f7, of: o7 } } = globalThis;
}
{
const { [(eff('k'), 'Array')]: { from: f8 }, ...rest } = globalThis;
}
{
const { [(eff('k'), 'Array')]: { prototype: { values: f2 } } } = globalThis;
}
{
const { [(eff('k'), 'at')]: a } = g();
}
{
const { [(eff('k'), 'from')]: f3 } = Array;
}
{
const { [(eff('k'), 'self')]: { Array: { from: f12 } } } = globalThis;
}
{
const { [(eff('k'), 'w')]: { [(eff('k2'), 'at')]: a } } = { w: src };
}
{
const { [(eff('k'), 'w')]: { [(eff('k2'), 'from')]: f } } = { w: Array };
}
{
const { [(eff('k'), 'w')]: { at: a } } = { w: [1] };
}
{
const { [(eff('k'), 'w')]: { at: f13 } } = { w: [1] };
}
{
const { [(eff('k'), 'w')]: { at: f19 } } = { w: g() };
}
{
const { [(eff('k'), 'w')]: { at: f20 } } = { w: arr };
}
{
const { [(eff('k'), 'w')]: { from: f4 } } = { w: Array };
}
{
const { [(eff('k'), 'w')]: { from: f4b }, z } = { w: Array, z: 1 };
}
{
const { [(eff(), 'Array')]: { [(eff(), 'from')]: f } } = globalThis;
}
{
const { [(eff(), 'Array')]: { from: f } } = globalThis;
}
{
const { [(eff(), 'Array')]: { prototype: { values: v2 } } } = globalThis;
}
