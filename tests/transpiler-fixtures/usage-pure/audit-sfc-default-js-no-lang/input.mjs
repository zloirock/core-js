// SFC script block without `lang=` token: Vue/Svelte/Astro default to JS when omitted.
// Bare `?vue&type=script` (or with `setup=true`) must reach polyfill emit even though the
// post-strip id has only the `.vue` extension.
arr.flat?.();
str.padStart(8);
