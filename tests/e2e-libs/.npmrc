# core-js is pinned to the workspace v4-alpha, which does not satisfy the
# `core-js ">= 3.0.0"` peerOptional of @rsbuild/core (prerelease semver), so a
# strict peer resolution errors. Use legacy (npm 6) peer behavior for this suite.
legacy-peer-deps=true
