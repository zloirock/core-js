// These proposals don't exist natively — pure imports MUST be injected by the plugin
export const clamp = 2.0.clamp(4, 6);
export const setFrom = Set.from([1, 2, 3]).size;
export const cooked = String.cooked`hello`;
