// default-phase unplugin leg: the whole transform (single 'pre') completes before
// babel lowers syntax and modules
import buildConfig from './webpack.usage-pure-unplugin-base.mjs';

export default buildConfig();
